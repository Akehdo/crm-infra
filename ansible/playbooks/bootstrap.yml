---
- name: Bootstrap CRM server
  hosts: crm_servers
  # Run this playbook with the initial root SSH access from Terraform. The role
  # creates the deploy user and configures sudo for later playbooks.
  become: false
  roles:
    - role: bootstrap
      tags:
        - bootstrap